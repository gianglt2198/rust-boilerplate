# # Tempo 2.10.x Configuration - Official Documentation Compliant
# # https://grafana.com/docs/tempo/latest/configuration/

# multitenancy_enabled: false

# server:
#   http_listen_address: 0.0.0.0
#   http_listen_port: 3200
#   grpc_listen_address: 0.0.0.0
#   grpc_listen_port: 9095
#   log_level: info
#   log_format: json
#   grpc_server_max_recv_msg_size: 16777216
#   grpc_server_max_send_msg_size: 16777216

# # Distributor - Receives traces from applications
# distributor:
#   receivers:
#     otlp:
#       protocols:
#         grpc:
#           endpoint: 0.0.0.0:4317
#         http:
#           endpoint: 0.0.0.0:4318

# # Ingester - Processes and stores traces
# ingester:
#   lifecycler:
#     ring:
#       replication_factor: 1
#       kvstore:
#         store: inmemory
#   trace_idle_period: 10s
#   trace_live_period: 5m
#   max_block_bytes: 524288000  # 500MB
#   max_block_duration: 30m
#   flush_all_on_shutdown: true

# # Metrics Generator - Generates metrics from traces
# metrics_generator:
#   ring:
#     kvstore:
#       store: inmemory
#   processor:
#     service_graphs:
#       histogram_buckets: [0.1, 0.2, 0.4, 0.8, 1.6, 3.2, 6.4, 12.8]
#       dimensions: [service.name, service.namespace]
#     span_metrics:
#       histogram_buckets: [0.002, 0.004, 0.008, 0.016, 0.032, 0.064, 0.128, 0.256, 0.512, 1.024, 2.048, 4.096, 8.192, 16.384]
#   registry:
#     collection_interval: 15s

# # Query Frontend - Handles search and query requests
# query_frontend:
#   max_retries: 3
#   search:
#     concurrent_jobs: 1000
#     target_bytes_per_job: 104857600  # 100MB
#     default_result_limit: 20
#     max_result_limit: 1000
#     query_backend_after: 15m
#     query_ingesters_until: 30m

# # Querier - Fetches traces from storage
# querier: {}

# # Storage Configuration
# storage:
#   trace:
#     backend: local
#     local:
#       path: /var/tempo/wal

# # Compactor - Compacts blocks
# compactor:
#   ring:
#     kvstore:
#       store: inmemory
#   compaction:
#     max_compaction_objects: 100
#     block_retention: 1000h

# # Overrides - Per-tenant configuration and limits
# # overrides:
# #   per_tenant_override_config: /etc/tempo/overrides.yaml

# # Ingester client compression (gRPC)
# ingester_client:
#   grpc_client_config:
#     grpc_compression: snappy

# # Metrics generator client compression
# metrics_generator_client:
#   grpc_client_config:
#     grpc_compression: snappy


stream_over_http_enabled: true
server:
  http_listen_port: 3200
  log_level: info

cache:
  background:
    writeback_goroutines: 5
  caches:
    - roles:
        - frontend-search
      memcached:
        addresses: memcached:11211

query_frontend:
  search:
    duration_slo: 5s
    throughput_bytes_slo: 1.073741824e+09
    metadata_slo:
      duration_slo: 5s
      throughput_bytes_slo: 1.073741824e+09
  trace_by_id:
    duration_slo: 100ms
  metrics:
    max_duration: 120h # maximum duration of a metrics query, increase for local setups
    query_backend_after: 5m
    duration_slo: 5s
    throughput_bytes_slo: 1.073741824e+09

distributor:
  receivers: # this configuration will listen on all ports and protocols that tempo is capable of.
    otlp:
      protocols:
        grpc:
          endpoint: "tempo:4317"
        http:
          endpoint: "tempo:4318"

ingester:
  max_block_duration: 5m # cut the headblock when this much time passes. this is being set for demo purposes and should probably be left alone normally

compactor:
  ring:
    kvstore:
      store: inmemory
  compaction:
    max_compaction_objects: 100
    block_retention: 1000h

metrics_generator:
  registry:
    external_labels:
      source: tempo
      cluster: docker-compose
  storage:
    path: /var/tempo/generator/wal
    remote_write:
      - url: http://prometheus:9090/api/v1/write
        send_exemplars: true
  traces_storage:
    path: /var/tempo/generator/traces
  processor:
    local_blocks:
      filter_server_spans: false
      flush_to_storage: true

storage:
  trace:
    backend: local # backend configuration to use
    wal:
      path: /var/tempo/wal # where to store the wal locally
    local:
      path: /var/tempo/blocks

overrides:
  defaults:
    metrics_generator:
      processors: [service-graphs, span-metrics, local-blocks] # enables metrics generator
      generate_native_histograms: both
